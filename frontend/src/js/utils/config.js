// Configuración global del frontend.
// Si en producción mueves la API a otro dominio, sólo cambias API_BASE.
export const API_BASE = `${window.location.protocol}//${window.location.host}/api/v1`;
export const APP_NAME = "Juridica · Colbeef";
